/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "images.pexels.com",
      "www.google.com",
      "unsplash.com",
      "www.pexels.com",
    ],
  },
};

module.exports = nextConfig;
